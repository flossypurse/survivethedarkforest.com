/**
 * Dark Forest Simulation Harness — placeholder
 *
 * TODO: Rewrite for exploration mode with spatial movement,
 * flashlight battery, multiple fires, weapon upgrades.
 * The old static-game harness is no longer compatible.
 */

console.log("Simulation harness needs rewrite for exploration mode.");
console.log("Old static game engine has been removed.");
process.exit(0);
